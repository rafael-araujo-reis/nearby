import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    maxHeight: 36,
    position: "absolute",
    top: 64,
    zIndex: 1,
  },
  content: {
    gap: 8,
    paddingHorizontal: 24,
  },
});
